# Reflexive Environment Variables
# Copy this file to .env and fill in your values

# Required: Claude API access
# Option 1: API Key (for production deployments)
ANTHROPIC_API_KEY=sk-ant-xxx

# Option 2: OAuth (for development, handled by Claude Code CLI)
# No environment variable needed - use `claude auth login`

# Server Configuration
PORT=3099
NODE_ENV=production

# Hosted Mode: API Authentication
# Generate a secure random key for your deployment
REFLEXIVE_API_KEY=your-secure-api-key-here

# Hosted Mode: Snapshot Storage (S3/R2)
# Required for snapshot persistence in hosted mode
AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-key
REFLEXIVE_SNAPSHOT_BUCKET=reflexive-snapshots

# Optional: S3-compatible endpoint (for Cloudflare R2, MinIO, etc.)
# REFLEXIVE_S3_ENDPOINT=https://your-account.r2.cloudflarestorage.com

# Optional: AWS Region (default: us-east-1)
# AWS_REGION=us-east-1
